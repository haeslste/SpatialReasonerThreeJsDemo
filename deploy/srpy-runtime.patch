diff --git a/src/SpatialReasoner.py b/src/SpatialReasoner.py
index c88d800..4ac903a 100644
--- a/src/SpatialReasoner.py
+++ b/src/SpatialReasoner.py
@@ -280,7 +280,10 @@ class SpatialReasoner:
         Check if the subject has a specific predicate relation with the object at with_obj_idx.
         """
         for relation in self.relations_of(with_obj_idx):
-            if relation.subject == subject and relation.predicate.value == have:
+            # Accept both human-readable values and compact enum names.
+            if relation.subject == subject and (
+                relation.predicate.value == have or relation.predicate.name == have
+            ):
                 return True
         return False
 
diff --git a/src/__init__.py b/src/__init__.py
index 100c58d..5276815 100644
--- a/src/__init__.py
+++ b/src/__init__.py
@@ -1,3 +1,11 @@
+import sys
+
+from . import vector3 as _vector3
+
+# Support SRpy's historical uppercase import on case-sensitive filesystems.
+sys.modules.setdefault(f"{__name__}.Vector3", _vector3)
+
+from .vector3 import Vector3
 from .BBoxSector import BBoxSector, BBoxSectorFlags
 
 from .SpatialBasics import (
@@ -34,5 +42,10 @@ from .SpatialPredicate import (
 )
 
 from .SpatialObject import ( SpatialObject )
-from .Exporter import (SceneExporter)
-from .SpatialRelation import SpatialRelation 
\ No newline at end of file
+from .SpatialRelation import SpatialRelation
+
+# The symbolic runtime does not require the optional USD/trimesh exporter.
+try:
+    from .Exporter import SceneExporter
+except ModuleNotFoundError:
+    SceneExporter = None
